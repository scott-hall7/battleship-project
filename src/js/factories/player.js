import Gameboard from './gameboard';

const Player = (name) =>    {
    const gameboard = Gameboard(name);
    
    const attackOpposingShip = (coordinates) => {
        
    }
    return {gameboard, attackOpposingShip}
}

export default Player;